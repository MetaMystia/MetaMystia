# 顾客同步审计

审计基线：`0a6c7bb`。仅普通营业，包含 DLC 对普通营业的影响；不含手动生成链、挑战模式，符卡仅检查流程边界。

证据为源码审计，不代表游戏实测。原版参考 `GuestsManager`、`GuestGroupController`、普通/稀客控制器；逆向仓库位置见本地配置。

## 问题与修复评估

| 编号 | 触发时序与后果 | 最小修复及验收 | 状态 |
| --- | --- | --- | --- |
| 1 | 主机桌上耐心耗尽，客机调用 `PatientDepletedLeave` 被自己的 Prefix 跳过，却进入 Leaving；桌位残留。 | 放行完整重放链，检查订单、耐心登记和桌位释放。小改动，无协议变化。 | 未修复 |
| 2 | 客机处理 MoveToDesk 后已到座，SendFromQueue 才到达；只接受 SeatMoving，后续点单堵塞直至超时。无需 TCP 乱序。 | 合并出队收尾或允许同次到座后的幂等收尾；延迟第二包仍能点单。小至中等改动。 | 未修复 |
| 3 | 旧空确认在评价后等待；下一单生成时 FSM 在压栈前进入 WaitingServe，旧序号仍匹配，重入旧订单评价，提前返回的 Postfix 又可能 Kill。非空确认通常被槽位检查拒绝。 | 主机拒绝过期请求，订单提交后才开放服务，评价 Postfix 区分提前返回。中等改动。 | 未修复 |
| 4 | 座满且队满：主机广播 Spawn 后直接 MoveToSpawn；客机停在 Constructed，没有 Pending 因而不会超时。 | 同步未入座离开，不能用依赖桌位的 LeaveFromDesk 代替；检查双方实体及登记。中等改动，需要消息。 | 未修复 |
| 5 | PlayerRepell 入口先广播并终结 FSM，原版却可能拒绝或等待异步回调；RoomRelay 还在主机判断前转发请求。 | 请求交主机校验；真正驱赶后同步完整清理，避免客机重新裁定、重复付款及赶走同桌新顾客。 | 已修复并构建，双端待实测 |
| 6 | PostEvaluation 返回后进入 ContinueDecision，但吃饭尚未结束；主机下一单先到，客机旧回调落在新单期间。 | 在实际续单入口放行；慢客机必须先完成上一单。中等改动，无协议变化。 | 未修复，条件性风险 |
| 7 | 广播 TryOverrideEvaluateByBuff 的中间结果，随后彩蛋/回调仍可改判。双端后续条件不同才可能分歧。 | 确定最终结果和副作用的分界；不能只改 PostEvaluation 参数，旧回调仍可能绑定旧分支。中至大改动。 | 未修复，条件性风险 |
| 8 | 正常离场不移除 GuestsMap；旧 Pending 可跨场景继续执行。真实时间超时也未区分暂停。 | 追加原版离场完成回调、场次结束清理及旧回调失效检查；连续营业不累积。中等改动。 | 未修复 |
| 9 | 双端分别随机初始化 Mood；续单时客机跳过余额扣减。 | 在正确时点同步心情及结转余额，不复制扣款公式；覆盖免费单。中等改动，有协议变化。 | 未修复 |

## 代码入口

- [GuestFSM](../src/MetaMystia.Mod/Managers/GuestFSM.cs)：状态、等待队列、请求执行。
- [GuestsManagerPatch](../src/MetaMystia.Mod/Patches/NightScene/GuestsManagerPatch.cs)：原版业务拦截。
- [GuestService](../src/MetaMystia.Mod/Managers/GuestService.cs)：客机重放。
- [Guest Actions](../src/MetaMystia.Mod/Network/Actions/WorkScene/Guest)：消息方向及数据。

## 实施顺序

原建议顺序为 1/2/3 → 4/8 → 6/9 → 5/7。按本次需求优先处理第 5 项，其余保持记录，不顺带修复。

## 第 5 项重新审计

- 原版 `CheckCanPlayerRepelGuest` 检查可赶客登记、全局许可和顾客自身许可；请求必须同时核对 RuntimeId 对应的控制器仍占据该桌。
- `PlayerRepell` 包含提前返回和异步回调，不能在入口声明成功；计数 Buff、扣心情、付款和排队耐心随机变化由主机原版执行。
- `RepellInternal` 注销订单、交互、耐心，再调用 `LeaveFromDesk`；只重放末端会漏清理。
- `PlayerRepellAction` 应成为仅主机接收的请求，不进等待队列、不自动转发。过期请求应直接结束。
- 主机真正进入驱赶后，在离桌入口发送驱赶结果；客机重放 `RepellInternal`，不重跑 `PlayerRepell`。
- 特殊符卡的表现和完整状态同步不在本次范围；不把拒绝或异步等待伪装成已离场。

## 验证目标

- 主机和客机发起普通赶客，主机执行一次，客机完整清除对应顾客的订单、耐心及桌位。
- 原版拒绝时没有成功广播，不终结 FSM；异步赶客在实际清理时才广播。
- 重复、过期请求及同桌换客不会影响新顾客。
- 主机结果不被旧服务 Pending 堵塞；客机正在打开的服务面板安全关闭。
- 编译结果与游戏双端实测分别记录。

## 第 5 项实现与验证

实现链：客机 PlayerRepell → 请求 → 主机校验控制器、桌位及 CheckCanPlayerRepelGuest → 原版 PlayerRepell → 实际 RepellInternal → 离桌入口广播 GuestRepellAction → 客机直接重放 RepellInternal。

- 请求不再 RoomRelay，也不进入顾客 Pending。失效请求直接结束，不等待下一轮订单。
- 主机本地赶客也检查原版许可。提前返回不会推进 FSM；异步分支不再依赖跨回调保存的全局放行次数。
- 在实际驱赶时设置 IsRepelling，关闭当前上菜面板；阻止关闭面板再次确认上菜或评价。无面板时不留下放行许可。
- 客机结果只接受主机，绕过旧服务 Pending，并检查同桌是否已换成另一顾客。重复终态结果不重复清理。
- 原版 RepellInternal 保留订单、交互、耐心及离桌清理，内部 LeaveFromDesk 只放行一次；不再同时广播普通 GuestLeaveAction。
- 同步清理入口处的主机心情、连击和连击损失计数，刷新客机连击及音乐状态；付款继续使用已有收入同步。
- 本次没有统一所有原版副作用：赶客后对排队顾客随机扣耐心仍由主机决定，客机耐心显示可能不同；逐出成就、剧情表现和符卡回调不在本轮适配范围。该限制不能作为完整原版双端等价的证明。
- 沿用已有 Left 的“开始离桌即终态”语义，第 8 项的实际离场注销仍未修复。
- 版本改为 0.27.1，以现有版本预检阻止与旧协议混用。旧 47 个 Action 编号保持不变，GuestRepell 追加为 47。

验证记录：

- [x] 对照原版正常、提前返回、异步驱赶及完整清理的源码调用链。
- [x] 使用实际 Interop 引用完整构建，0 警告、0 错误。
- [x] 静态检查旧 Action 编号不变，48 个 Union 一一对应且无重复。
- [x] git diff --check 通过。
- [ ] 双端正常驱赶、拒绝、重复请求、同桌换客、慢客机及面板关闭实测。

构建输出为本地隔离产物，未部署、未启动游戏。本轮没有游戏运行时或自动化行为测试结果。
